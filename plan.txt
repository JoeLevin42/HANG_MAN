Planing: by the draw
=====
init:
choosen_word = random from list of words
shown_var = "The represented var" shows len(choosen_word) * "-"
guess_counter = starts with number and goes down every wrong guess 
gusses history = empty list
flag = true/false (the counter zero)
==
print:
-print every move the gusses couner how much remain
-the guess counter
-print the shown_var - with updating
==
Flow:
while if flag:
-User Input
-check the if the letter in the choosen words?
!if yes = update the {shown_var}
==how to update the shown var? = go on the choosen word and check with the user Input
what the indexes of the letter , save it in dict with {key=letter:val=[index1,index2]}
then go to the dict key nad check the indexex then update the letter in the indexex on
the shown_var  
if the letter relvald check = if the shown_var == choosen word


=
if no = counter of guess -= 1 
 

